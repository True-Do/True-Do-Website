import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mulitate4.truedo',
  appName: 'true-do-website-next',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
};

export default config;
